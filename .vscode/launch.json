{
    "version": "0.2.0",
    "configurations": [
        {
            // ★ 変更：分かりやすいように名前を変更
            "name": "(Windows) Launch PaintApp", 
            // ★ 変更：デバッガの種類を "cppvsdbg" に変更
            "type": "cppvsdbg",               
            "request": "launch",            
            
            "program": "${command:cmake.launchTargetPath}",

            "args": [], 
            
            "stopAtEntry": true, 
            
            "cwd": "${workspaceFolder}", 
            "environment": [],
            // ★ 変更：外部コンソールを true にすると、デバッグメッセージが見やすい場合があります
            "externalConsole": true 
            
            // ★ 削除：MIModeとmiDebuggerPathはGDB用の設定なので不要です
        }
    ]
}