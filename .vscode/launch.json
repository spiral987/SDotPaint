{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch SDotPaint",
            "type": "cppvsdbg",
            "request": "launch",
            
            // ★ 重要：実際のexeファイルの直接パスを指定
            "program": "${workspaceFolder}/build/Debug/SDotPaint.exe",

            "args": [],
            
            "stopAtEntry": false,
            
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false
        }
    ]
}